% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection_duckdb.r
\name{duckdbCon}
\alias{duckdbCon}
\title{Create a DuckDB-backed OMOP connection}
\usage{
duckdbCon(dbdir, read_only = TRUE, ...)
}
\arguments{
\item{dbdir}{Character scalar. Path to the DuckDB database directory.}

\item{read_only}{Logical scalar. Whether to open the database read-only.}

\item{...}{Additional arguments passed to \code{DBI::dbConnect()}.}
}
\value{
A list with one entry per available OMOP table plus a \code{dbcon}
element containing the DuckDB connection.
}
\description{
Build a list of \code{tbl} objects for available OMOP tables along with the
underlying DuckDB connection.
}
\examples{
db_path <- tempfile(fileext = ".duckdb")
con <- duckdbCon(db_path)
DBI::dbDisconnect(con$dbcon, shutdown = TRUE)
}
